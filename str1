-구조체
서로 다른 데이터 타입(정수, 실수, 문자열)을 하나로 모으려면

struct student
    {
        int number;
        char name[20];
        double grade;

    };
struct student s; //변수선언
s.number;//구조체 성분

struct point 
{
    int x; 
    int y;
}
같은 구조체간의 대입은 가능 ==> p1 = p2
비교는 불가 ==> p1 == p2
but, p1.x == p2.x ==> 가능

-공용체
선언하고 사용하는 방법은 구조체와 유사

union example
{
    char c;
    int i;
};

-열거형
변수가 가질 수 있는 값들을 미리 열거해

-typedef
typedef unsigned char BYTE;
BYTE index;










